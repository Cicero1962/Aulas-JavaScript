var nome = window.prompt('Qual é seu nome?')
document.write(`Olá <strong>${nome}</strong> ! Seu nome tem ${nome.length} letras.<br/>`)
document.write(`Seu nome em maiúsculo é ${nome.toLocaleUpperCase()}<br/>`)
document.write(`Seu nome em minúsculo é ${nome.toLocaleLowerCase()}`)

/*s.length (quantos caracteres a string tem)
  s.toUpperCase()-(tudo em letras MAIÚSCUKAS)
  S.toLowerCase() (tudo em letras minúsculas)
  write (escreva)
   n1.toLocaleString('pt-BR', {style: 'currency', currency: 'BRL'})*/

  
   

   


